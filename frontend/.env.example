# Build-time bases for split-origin deployments (static site + API service).
# Leave unset in dev — the Vite proxy serves same-origin /api and /ws
# (see vite.config.ts). render.yml sets these for production.
VITE_API_BASE=
VITE_WS_BASE=
