# Runs the backend test suite in isolation.
# Tests use sqlite + eager Celery + in-memory channels, so no db/redis services
# are needed — everything runs inside one container.
services:
  test:
    build: ./backend
    environment:
      DJANGO_SETTINGS_MODULE: config.test_settings
    command: sh -c "pytest -q"
